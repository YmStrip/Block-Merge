//Just Mcfunction
exports .main = function ( yc ) {
	let merge = function ( str , max , split , call ) {
		str = str .split ( "\n" )
		let obj = { }
	z )

	
		}
		obj .merges = function ( D , e ) {
			let E = e
	 b , d ) {
				let p = D [ x + " " + y + " " + z ]
				if ( p ) {
					if ( p [ 0 ] == b && p [ 1 ] == d ) {
						return true
					}
			
						let l3 = true
						if ( Dy !== 0 ) {
							l3 = side ( sx, j, sz, i-Dx, j, k-Dz, b, d , "l3" )
							
							//console.log("y",l3)
				
				//console.log(sx,sy,sz,dx,dy,dz)
				if ( dx !== 0 || dy !== 0 || dz !== 0 ) {
			
					obj .res .push ( "setblock ~" + sx + " ~" + sy + " ~" + sz + " " + b + " " + d )
				}
			}
			//console.log(e,obj.end,obj.res)
			//console.log(E,obj.end)
			obj .p += 1
			call ( {
				type: "p",

					use: obj .use,
					len: obj .len,
					//obj,
					alldata: obj .data
				} )
				//console.log(obj)

				}
			}
		}
		/*
		let P = { d: obj }
		console.log(P)

		//console.log()
	}
	//解析指令
	let merge_a = function ( s ) {
		//fill~~~air0

				pos: [ num ( s [ 1 ] ) , num ( s [ 2 ] ) , num ( s [ 3 ] ) , num ( s [ 4 ] ) , num ( s [ 5 ] ) , num ( end [ 0 ] ) ],
				type: "fill",
				b: end [ 1 ],
				d: num ( end [ 2 ] )
	os [ 2 ] , d .pos [ 5 ] )
			//console.log(x,y,z)
			for ( let i = x .min; i <= x .max ; i ++ ) {
			 <= z .max ; k ++ ) {
						//console.log(i,j,k)
						if ( i <= x .max || j <= y .max || k <= z .max ) {
							
			
				}
			}
	let num = function ( str ) {
		//str = str .replace ( /\s+/g, "" )
		if ( str == "" ) {
			return 0
		}
		return Number ( str )		let max = arguments [ 0 ]
		for ( let i in arguments ) {

	//坐标合并
	let num1 = function ( b ) {
		b = b / 20
		if ( b >= 0 ) {
			if ( b < Math .round ( b ) ) {

	yc .set ( "merge" , merge )
	
	merge ( yc.readfile('./test.txt','utf8'), 100000 , true , function ( res ) {
		if (res.type!=='end'){
			console.log(res)
		}
		if ( res .type == 'end' ) {
			yc .c ( 's' , '[16万彩色主城]三维优化测试成功，优化率:' + res .k )
			yc .writefile ( './m.txt' , res .data .join ( '\n' ) )
		}
	} )
	
}
// OH FUCK,THIS FILE WAS DELETE IN TOMMOROW
